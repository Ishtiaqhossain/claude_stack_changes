{
  "name": "pr-splitting-demo",
  "version": "1.0.0",
  "description": "Sample project demonstrating the splitting-changes-into-prs skill",
  "type": "module",
  "scripts": {
    "build": "node --check src/report.js && node --check src/index.js && node src/index.js > /dev/null && echo build ok",
    "test": "node --test"
  }
}
